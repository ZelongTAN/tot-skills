Use case: technical README banner for a developer skill.
Asset type: wide GitHub README hero banner, designed to be cropped safely into a 3:1 horizontal banner.
Primary request: Create a flat academic-style system diagram that compares the normal way of using one Codex with the Codex Collab method.

Scene/backdrop: a clean white paper-like canvas with a subtle grid, like a polished system architecture figure in a technical report. Use a left-right comparison layout with a thin vertical divider.

Left half, "Before" concept: show a single overloaded Codex session node receiving a complex task from one human icon, then trying to handle planning, research, coding, testing, and review by itself. Use a small stack of mixed responsibility cards and a dense context block to imply that everything is compressed into one conversation. Keep it simple and slightly strained, but not chaotic.

Right half, "After" concept: show one human icon talking only to a main Coordinator Codex node. The coordinator writes to a local JSON task state, dispatches parallel subtasks to three persistent worker session nodes, receives their handoff cards through a FIFO queue, updates a dashboard panel, and then returns a reviewed decision to the human. Use clean arrows and a queue path to make the callback mechanism visible.

Style/medium: flat 2D vector-like diagram, technical, academic, restrained, precise. No photorealism, no people in offices, no 3D render, no glossy SaaS illustration. Use crisp rounded rectangles, thin arrows, document icons, queue cards, and dashboard mini-panels. It should feel like a high-quality paper figure or engineering explainer.

Composition/framing: two equally sized panels, left "ordinary single-session flow", right "coordinator plus persistent workers". Put all important content in the central horizontal band so it survives a 1536x512 crop. Leave clean top margin for optional README title outside the image.

Color palette: mostly white, charcoal, light gray, muted blue, muted green, and a small amber accent. Avoid purple-heavy gradients, dark cyberpunk colors, and decorative background blobs.

Text: if text is used, keep it very sparse and perfectly readable. Use only these simple labels if possible: "Before", "After", "Coordinator", "Worker", "JSON", "Queue", "Dashboard". Do not use paragraphs or tiny labels.

Constraints: visually communicate that the user still talks to one main Codex, while the main Codex can coordinate persistent worker sessions, JSON-first task state, a durable callback queue, and dashboard review. Make the mechanism clear before making it beautiful.

Avoid: business presentation style, office scene, photorealistic person, 3D dashboard, mascot, robot, logo, neural network, sci-fi glow, dark hacker screen, messy decorative elements, tiny unreadable text, watermark.
